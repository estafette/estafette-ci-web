{{- range .Values.extraSecrets -}}
---
kind: Secret
apiVersion: v1
metadata:
  name: {{ include "estafette-ci-web.fullname" . }}.{{ .key }}
  labels:
    {{- include "estafette-ci-web.labels" . | nindent 4 }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 -}}
  {{- end }}
{{- if .data }}
data:
  {{- range $key, $value := .data }}
    {{ $key }}: {{ $value | toString | b64enc }}
  {{- end }}
{{- end }}
{{- end }}