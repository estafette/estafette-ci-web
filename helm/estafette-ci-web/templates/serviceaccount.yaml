{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "estafette-ci-web.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "estafette-ci-web.labels" . | indent 4 }}
{{- end -}}
